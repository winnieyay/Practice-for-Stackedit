# 註解，會被忽略。
# 不要追蹤檔名為 .o 結尾的檔案
*.o
# 但是要追蹤 lib.o，即使上方已指定忽略所有的 .o 檔案
!lib.o

# 只忽略根目錄下的 TODO 檔案。 不包含子目錄下的 TODO
# /TODO
# 忽略build/目錄下所有檔案
# build/
# 忽略doc/notes.txt但不包含doc/server/arch.txt
# doc/*.txt
# ignore all .txt files in the doc/ directory
# doc/**/*.txt
